services:
  postgres:
    image: postgres:16
    container_name: sigchi-postgres
    environment:
      POSTGRES_DB: sigchi
      POSTGRES_USER: sigchi_user
      POSTGRES_PASSWORD: sigchi_password
    ports:
      - "5432:5432"
    volumes:
      - ./postgres-data:/var/lib/postgresql/data

